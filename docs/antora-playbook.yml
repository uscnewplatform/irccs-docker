site:
  title: IRCCS Docker Platform
  start_page: docs::index.adoc

content:
  sources:
    - url: .               # ← Punto corrente (/antora nel container)
      start_path: .        # ← antora.yml è qui direttamente
      branches: HEAD

ui:
  bundle:
    url: https://gitlab.com/antora/antora-ui-default/-/jobs/artifacts/HEAD/raw/build/ui-bundle.zip?job=bundle-stable
    snapshot: true

output:
  dir: ./build/site

asciidoc:
  attributes:
    page-pagination: true
    experimental: ''