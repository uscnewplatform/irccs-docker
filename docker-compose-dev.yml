version: '3.1'

services:
  hapi-fhir-jpaserver-start:
    image: irccs/fhir-server:1.0.1
    restart: on-failure
    environment:
      hapi.fhir.allow_external_references: "true"
    ports:
      - "8080:8080"


  #   hapi-fhir-postgres:
  #     image: postgres:13-alpine
  #     container_name: hapi-fhir-postgres
  #     restart: always
  #     environment:
  #       POSTGRES_DB: "hapi"
  #       POSTGRES_USER: "admin"
  #       POSTGRES_PASSWORD: "admin"
  #     volumes:
  #       - hapi-fhir-postgres:/var/lib/postgresql/data
  # volumes:
  #   hapi-fhir-postgres:


  #fhir-api:
  #  image: "mcr.microsoft.com/healthcareapis/r4-fhir-server"
  #  restart: on-failure
  #  environment:
  #    FHIRServer__Security__Enabled: "false"
  #    SqlServer__ConnectionString: "Server=tcp:sql,1433;Initial Catalog=dbo;Persist Security Info=False;User ID=sa;Password=yourStrong(!)Password;MultipleActiveResultSets=False;Connection Timeout=30;TrustServerCertificate=false;Encrypt=False;"
  #    SqlServer__AllowDatabaseCreation: "true"
  #    SqlServer__Initialize: "true"
  #    SqlServer__SchemaOptions__AutomaticUpdatesEnabled: "true"
  #    DataStore: "SqlServer"
  #  ports:
  #    - "8080:8080"
#
  #sql:
  #  image: "mcr.microsoft.com/mssql/server:2019-latest"
  #  environment:
  #    MSSQL_SA_PASSWORD: "yourStrong(!)Password"
  #    ACCEPT_EULA: "Y"
  #  ports:
  #    - "1433:1433"

  mysql:
    image: "mysql:5.6"
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: irccs
      MYSQL_PASSWORD: irccs

  #mongo:
  #  image: mongo:5.0.5
  #  ports:
  #    - '27017:27017'
